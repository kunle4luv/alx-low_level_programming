#include "main.h"

/**
 * _abs - A function to compute the absolute value of an integer
 * @n: the int to check
 *
 * return: the absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
